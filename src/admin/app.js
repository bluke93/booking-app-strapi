const config = {
  locales: [
    'en',
    'it'
  ],
  translations: {
    it: {
      "components.Select.placeholder": "Select"
    },
    en: {},
  },
};

const bootstrap = (app) => {
};

export default {
  config,
  bootstrap,
};
